doctype html
html(lang='ru')
    block config
        include ../../config/config.pug
    head
        block head
            meta(charset='utf-8')
            title #{pageName} | DJ-STORE
            meta(name='description' content= pageDesc)
            link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
    body(class= bodyclass)
        block header

        block content

        block footer